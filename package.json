{
  "name": "json-explorer",
  "version": "0.0.1",
  "main": "dist/content.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/rmkane/json-tree.git"
  },
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "keywords": [
    "json",
    "tree",
    "viewer"
  ],
  "author": "Ryan Kane",
  "license": "MIT",
  "description": "A JSON explorer for the browser",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild"
    ],
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "dependencies": {
    "@types/chrome": "^0.0.308"
  }
}
